/**
 * @deprecated Re-exported from `data/elements`. New consumers should
 * import `ELEMENT_COLOR` from there directly. Kept here so the icon
 * painters that already import this path don't churn in one sweep.
 */
export { ELEMENT_COLOR } from './elements';
